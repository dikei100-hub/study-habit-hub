# 2026-08-22 세션 — CoreRules 모순 정리 및 안드로이드 규칙 흡수

지시서: `docs/orders/2026-08-22-docs-consolidation.md`
성격: 문서 전용 사이클. `src/` 무수정.

---

## 1. 착수 전 확인 (지시서 2절)

실행 결과 원문:

```
$ git log --oneline origin/main..HEAD
(출력 없음)

$ git status --short
 M src/routeTree.gen.ts
?? docs/orders/2026-08-22-docs-consolidation.md
```

- **로컬이 원격보다 앞서 있지 않다.** `origin/main..HEAD` 가 비어 있다.
  지난 사이클의 `86eb7b4` 를 포함한 4개 커밋 + 문서 커밋 2개는 모두 push 완료 상태.
- **`?? docs/CrossTrack_*.md` 는 나오지 않는다.** 두 파일은 커밋 `1fbcea1`
  ("문서: 안드로이드 트랙 참고 자료 추가")로 이미 추적 중이다. 지시서 2절의
  기대와 일치한다.
- `src/routeTree.gen.ts` 는 내용 차이 0인 채로 남아 있다. 스테이징하지 않는다.
- 이번 지시서 파일이 미추적 상태다. 지시서 0절에 따라 커밋 1에 포함한다.

## 2. 현재 구조 확인 (지시서 4절)

읽은 것: `docs/CoreRules.md` 전문, `docs/CrossTrack_Lessons.md` 1·2·3장,
`docs/CrossTrack_AppSpec.md` 4-4·5·7장, `docs/sessions/2026-08-21.md` 의
"남은 문서 모순" 절.

지정된 grep 결과:

```
$ grep -n "on 상태\|enabled\|매일 할지" docs/CoreRules.md
68:  새 항목은 on 상태로 시작하고, 대상 날짜의 할 일에도 즉시 나타난다.
70:- **체크박스**: 2번 항목 참고 (매일 할지 말지).
96:2. **하나도 없으면** → 리스트 관리 목록에서 **on 상태인 항목들**을
98:3. on 상태인 항목이 하나도 없으면 빈 목록으로 시작한다.
215:  2장 재정의(템플릿 `enabled` 제거)로 스키마가 바뀌면서 올렸다.
242:| 리스트 관리 체크박스가 별도 `enabled` 상태라 두 화면이 어긋남 (2장 재정의) | 이번 사이클 — 커밋 2 |
```

- 68·70·96·98 은 지시서 5절이 지정한 곳과 정확히 일치한다. **5절이 놓친 곳은 없다.**
- 215·242 의 `enabled` 는 "옛 스키마를 제거했다"는 **과거 사실 서술**이므로
  새 2장과 모순되지 않는다. 7절 완료 조건 grep 에도 `enabled` 는 없다. 그대로 둔다.
- 5절이 지정한 5번째 위치(5장 1항 "하나라도 있으면")는 95행에 있다.
- 2026-08-21 세션 문서의 "남은 문서 모순" 표(4행)는 이번 지시서 5절 표의
  앞 4행과 같다. 5장 1항이 이번에 추가된 항목이다.

## 3. 계획

### 커밋 1 — 문서: CoreRules 2장 재정의에 따른 나머지 장 정리

수정 파일: `docs/CoreRules.md`, `docs/orders/2026-08-22-docs-consolidation.md`(신규 추가)

| 행 | 현재 | 고칠 내용 |
|---|---|---|
| 26 | "켜져 있는 항목이 자동으로 깔림" | "리스트 관리 목록 전체가 자동으로 깔림" |
| 68 | "새 항목은 on 상태로 시작하고, 대상 날짜의 할 일에도 즉시 나타난다." | "새 항목은 대상 날짜의 할 일에 즉시 나타난다." (on/off 표현 제거) |
| 70 | "2번 항목 참고 (매일 할지 말지)" | "2장 참고 (그 날짜 목록에 들어 있는가)" |
| 95 | "할 일이 **하나라도 있으면** → 아무것도 하지 않는다" | "**그 날짜를 만든 적이 있으면** → 아무것도 하지 않는다" + 이유 한 줄 |
| 96 | "**on 상태인 항목들**을 복사" | "리스트 관리 목록 **전체**를 복사" |
| 98 | "on 상태인 항목이 하나도 없으면" | "목록이 비어 있으면" |

- 95행에 덧붙일 이유: 커밋 `e64b744` 로 판정 기준이 길이에서 키 존재로 바뀌었고,
  사용자가 그날 항목을 전부 지운 빈 목록도 "만든 적이 있는 것"이므로 다시 깔지 않는다.
- 5장 끝에 달력 표시와의 기준 차이 문단(지시서 5절 인용문) 추가.

### 커밋 2 — 문서: 보상 규칙을 안드로이드 최신 기준으로 갱신

수정 파일: `docs/CoreRules.md` (8장 전면 재작성 + 0장 1행)

- (가) 8장 맨 앞에 **습관 축 / 완벽 축** 분리 문단.
- (나) 스트릭 정의를 "연속 100% 달성" → **"연속으로 공부한 날(완료 ≥ 1)"** 로 완화.
  시작점 유예 / 방향과 종료 / 최고 기록 비감소 / 매번 재계산 4개 항목 포함,
  완화 이유 한 줄.
- (다) 0장 낡은 목록의 스트릭 행(24행)은 **삭제**한다. (나)로 낡은 쪽이 맞는
  정의가 되어 "낡은 내용 vs 실제"가 같아지므로 행 자체가 무의미해진다.
  ※ 지시서가 "삭제하거나 바로잡는다"로 선택지를 줬고, 삭제 쪽을 고른다.
- (라) 코인 절에 완벽 축 유지 + 비대칭이 의도된 것이라는 문단 추가.
- (마) 배지 표를 12행짜리 새 표(축 열 포함)로 교체 + 바뀐 점 2줄.
- (바) 8장 끝에 "판정 규칙" 절 신설 (완벽한 날 / 달성률 / 정산 순서 /
  배지 영속성 / 식별자 분리).
- (사) 프리즈 절을 규칙 포함으로 보강.

### 커밋 3 — 문서: 참고 자료의 위치와 웹과의 차이 명시

수정 파일: `docs/CrossTrack_AppSpec.md`, `docs/CoreRules.md`(0장),
`docs/sessions/2026-08-22.md`

- AppSpec 맨 위에 우선순위 헤더 + "웹 트랙이 다르게 정한 것"(설정 화면 / 4장 사양) 블록.
- CoreRules 0장 문서 우선순위 목록에 `CrossTrack_Lessons.md`·`CrossTrack_AppSpec.md` 추가.
- 세션 문서(이 파일)를 이 커밋에 포함한다.
  ※ 지시서가 세션 문서의 커밋 귀속을 지정하지 않았다. 3개 커밋의 결과를 모두
  담아야 하므로 마지막 커밋에 넣는 것으로 판단했다.

### 지키는 것

- `src/` 무수정. `src/routeTree.gen.ts` 스테이징 안 함 — `git add` 는 항상 경로 명시.
- `CrossTrack_Lessons.md` 본문 무수정. `FunctionalSpec` / `ScreenDesign` /
  `RewardSystemDesign` / `DateBasedTodoDesign` 무수정.
- `prettier --write` 전체 실행 안 함. push 안 함. amend/rebase/force 안 함.
- 지정된 곳만 수정. 톤·구조 임의 재편 없음.

## 4. 지시서에 없는 모순 — 보고만 하고 고치지 않음 (지시서 4절·7절)

| # | 위치 | 내용 | 왜 문제인가 |
|---|---|---|---|
| 1 | `CoreRules.md` 10장 해결됨 표 242·243행 | 커밋 칸이 아직 `이번 사이클 — 커밋 2` / `커밋 3` 인 채로 남아 있다 | 실제 해시는 `148e1bf`·`e64b744` 다. 2026-08-21 세션 문서 580행이 "해시가 아직 없어 그렇게 적었다"고 남겼는데 갱신되지 않았다 |
| 2 | `CoreRules.md` 0장 15행 | `RewardSystemDesign.md` 를 우선순위 2위·"비교적 최신"으로 둔다 | 커밋 2 로 8장이 안드로이드 최신 기준을 흡수하면 `RewardSystemDesign.md` 는 개편 **이전** 기준(연속형 큰 배지 등)이 되어 사실상 낡은 문서가 된다. 우선순위 2위가 더는 맞지 않는다 |
| 3 | `CoreRules.md` 8장 206행 | 프리즈 절이 "나중에 붙일 때 `RewardSystemDesign.md` 4장 참고" 로 안내 | 커밋 2 (사)가 규칙을 본문에 직접 적으면 이 포인터를 남길지 지울지 지시서에 없다. 일단 규칙만 추가하고 포인터는 그대로 둘 계획이다 |
| 4 | `CoreRules.md` 12장 268행 | `실행: npm run dev` | 저장소의 커밋된 락파일은 `bun.lock` 이고 `bunfig.toml` 이 설치 정책을 잡는다. `CLAUDE.md` 기준 명령은 `bun run dev` 다. (실제로는 둘 다 `vite dev` 를 돌리므로 동작 차이는 없다) |
| 5 | 지시서 5절 (나) vs `CrossTrack_Lessons.md` 3-4 | 새 스트릭 정의에 **"또는 그날이 방어(프리즈)된 날"** 조건이 빠져 있다 | 프리즈가 웹에 미구현이라 지금은 무해하지만, (사)의 프리즈를 실제로 붙이는 순간 스트릭 정의에 이 절이 없으면 방어한 날이 이어지지 않는다. 지시서대로 빼고 쓰되, 프리즈 구현 사이클에서 반드시 함께 넣어야 한다 |

## 5. 사용자 확인 결과 — 계획 승인 + 지시 3건 추가

지시서 1절에 따라 계획 보고 후 멈췄고, 사용자가 승인하며 아래 3건을 덧붙였다.
지시서 본문은 읽기 전용이므로 고치지 않고 여기에 기록한다.

1. **(나) 첫 항목을 프리즈 조건 포함으로 쓴다.** 위 4절 5번(프리즈 조건 누락)은
   지시서의 실수라는 판단. 확정 문안:

   > - **연속으로 "인정되는 날"이 이어진 수.**
   >   하루가 인정되는 조건: 그날 완료 개수 ≥ 1, **또는** 그날이 방어(프리즈)된 날.
   >   완료 개수는 그날 예정 개수와 무관하다.
   >   (프리즈는 웹 미구현. 붙일 때를 위해 조건을 미리 적어둔다.)

   → 4절 5번은 이로써 해소된다.

2. **4절 1번(10장 242·243행)을 실제 해시로 고친다.** `이번 사이클 — 커밋 2/3`
   → `148e1bf` · `e64b744`. **커밋 1에 포함.**

3. **4절 2번(0장 `RewardSystemDesign.md` 우선순위)을 커밋 3에서 처리한다.**
   2위에서 내리고 "보상 개편 이전 기준. 프리즈 상세만 참고" 단서를 단다.

4절 3번(프리즈 포인터)·4번(npm/bun)은 **그대로 둔다.**

---

## 6. 실행 기록

### 커밋 1 — `ec8be70` 문서: CoreRules 2장 재정의에 따른 나머지 장 정리

수정: `docs/CoreRules.md`, 신규 추가: `docs/orders/2026-08-22-docs-consolidation.md`

- 0장 낡은 목록: "켜져 있는 항목이" → "리스트 관리 목록 전체가".
- 3장 추가: "새 항목은 on 상태로 시작하고" 를 걷어내고
  "새 항목은 대상 날짜의 할 일에 즉시 나타난다" 로.
- 3장 체크박스: "2번 항목 참고 (매일 할지 말지)" → "2장 참고 (그 날짜 목록에
  들어 있는가)".
- 5장 1항: "하나라도 있으면" → **"그 날짜를 만든 적이 있으면"**.
  판정 기준이 개수가 아니라 날짜 키의 존재이며 빈 목록도 포함된다는 두 줄을 덧붙였다.
- 5장 2·3항: "on 상태인 항목들" → "목록 전체", "on 상태인 항목이 하나도 없으면"
  → "목록이 비어 있으면".
- 5장 끝에 달력 표시(빈 날은 `-`, 스트릭 끊김)와 재시딩 판정의 기준이 다른
  이유 문단 추가.
- 10장 해결됨 표: `이번 사이클 — 커밋 2/3` → `148e1bf` / `e64b744` (승인 2번).

### 커밋 2 — `212e00d` 문서: 보상 규칙을 안드로이드 최신 기준으로 갱신

수정: `docs/CoreRules.md`

- (가) 8장 맨 앞에 습관 축 / 완벽 축 분리 문단.
- (나) 스트릭을 완화. 첫 항목은 승인 1번의 확정 문안(프리즈 조건 포함)을 썼다.
  시작점 유예 · 방향과 종료 · 최고 기록 비감소 · 매번 재계산 + 완화 이유 인용구.
- (다) 0장 낡은 목록의 스트릭 행 **삭제**.
- (라) 코인 절 끝에 완벽 축 유지와 비대칭이 의도된 것이라는 문단.
- (마) 배지 표를 축 열 포함 12행 새 표로 교체 + "바뀐 점" 2줄.
- (바) "판정 규칙" 절 신설 — 완벽한 날 / 달성률 / 정산 순서 / 배지 영속성 /
  식별자 분리.
- (사) 프리즈 절에 대상·제안 조건·적용 순서·원자성·보상 없음 5개 항목 추가.
  기존 `RewardSystemDesign.md` 4장 포인터는 승인대로 그대로 뒀다.

> **표현 조정 — 지시서 5절 (마)와 7절 완료 조건의 충돌**
> (마)는 바뀐 점에 옛 배지 이름 두 개를 그대로 적으라고 했는데, 7절 완료 조건은
> `grep "독서광\|학습 마스터"` 가 비어 있어야 한다고 요구한다. 둘을 동시에
> 만족할 수 없다. 2026-08-21 사이클에서 `enabled` 주석을 같은 이유로 고쳐 쓴
> 선례에 따라, 정보는 남기되 옛 이름 문자열은 쓰지 않는 문장으로 바꿨다
> ("누적 완료 개수 계열의 옛 이름 3종 → 계열이 왕/광/마스터로 뒤죽박죽이었고
> 그중 하나는 독서 앱에서나 쓸 말이라…"). 검사가 실제로 깨끗해지는 쪽을 골랐다.

### 커밋 3 — 문서: 참고 자료의 위치와 웹과의 차이 명시

수정: `docs/CrossTrack_AppSpec.md`, `docs/CoreRules.md`, 신규: 이 파일.

- AppSpec 맨 위에 우선순위 헤더와 "웹 트랙이 다르게 정한 것"(설정 화면 · 4장
  화면 사양) 블록을 붙였다. `CrossTrack_Lessons.md` 본문은 손대지 않았다.
- CoreRules 0장 우선순위 목록에 참고 자료 두 줄 추가.
- CoreRules 0장에서 `RewardSystemDesign.md` 를 2위에서 3위로 내리고
  "보상 개편 이전 기준. 프리즈 상세만 참고" 단서를 달았다 (승인 3번).
  `DateBasedTodoDesign.md` 가 2위로 올라왔다.

> 커밋 3의 해시는 이 파일이 그 커밋에 포함되므로 여기에 적을 수 없다.
> 화면 보고로 대신한다.

## 7. 완료 조건 확인 (지시서 7절)

- `git status` / `git diff --stat` — 변경은 `docs/` 아래뿐. **`src/` 없음.**
  `src/routeTree.gen.ts` 는 내용 차이 0이라 `git diff --stat` 에 나타나지도 않고,
  세 커밋 모두 경로를 명시해 `git add` 했으므로 스테이징되지 않았다.
- `grep -n "on 상태\|매일 할지\|하나라도 있으면" docs/CoreRules.md` → 비어 있음.
- `grep -n "독서광\|학습 마스터\|100% 달성한 날" docs/CoreRules.md` → 비어 있음.
- 커밋 3개. 합치지 않았다.
- 지시서에 없는 모순은 4절에 목록으로 보고했고, 그중 1·2번은 승인에 따라
  이번에 고쳤다. 3·4번은 그대로 뒀다. 5번은 승인 1번으로 해소.

### 작업 중 새로 발견한 것

- 작업 도중 `docs/orders/CrossTrack_Assets.md` 와 `docs/orders/drawable.zip` 이
  미추적 상태로 나타났다. 착수 전 확인(1절) 시점에는 없었다. 이번 지시서의
  수정 허용 범위 밖이므로 **어느 커밋에도 넣지 않고 그대로 뒀다.**

## 8. 사이클 후속 — 에셋 목록 문서 (지시서 밖, 사용자 요청)

커밋 `589fab2` "문서: 이미지 에셋 목록 추가".

- `docs/CrossTrack_Assets.md` 신규 추가(사용자가 안드로이드 트랙에서 가져온 것).
- `docs/CoreRules.md` 0장 참고 자료 목록에 한 줄 추가. 배지 **파일명이 옛
  코드명이라 현재 배지명과 다르다**는 주의를 함께 적었다.
- 에셋 문서의 배지 12종 이름이 커밋 `212e00d` 로 교체한 8장 배지 표와 일치한다.
- `public/drawable*` 는 손대지 않았다.
- 보고: `CoreRules` 7장의 "마스코트와 배지 이미지는 아직 없다" 와 에셋 문서
  5절 체크리스트가 충돌한다. → 아래 두 번째 사이클 커밋 3에서 처리한다.

---

# 두 번째 사이클 — PWA 설치 대응 및 설정 시트 신설

지시서: `docs/orders/2026-08-22-pwa-install.md`

## 1. 착수 전 확인 (지시서 2절)

```
$ git log --oneline origin/main..HEAD
(출력 없음)

$ git status --short
 M src/routeTree.gen.ts
?? docs/orders/2026-08-22-pwa-install.md
?? public/apple-touch-icon.png
?? public/drawable-nodpi/
?? public/drawable/
?? public/icon-192.png
?? public/icon-512.png
?? public/icon-maskable-512.png
```

- 로컬이 원격보다 앞서 있지 않다. `589fab2` 까지 push 완료.
- **아이콘 PNG 4개가 모두 있다.** `apple-touch-icon.png`(30KB) ·
  `icon-192.png`(32KB) · `icon-512.png`(178KB) · `icon-maskable-512.png`(118KB).
  기존 `favicon.ico` 도 그대로 있다.
- `public/drawable/`·`public/drawable-nodpi/` 는 건드리지 않는다. `drawable.zip`
  은 이미 사라졌다(사용자가 정리한 것으로 보임).
- 지시서 파일이 미추적 상태다. 0절에 따라 커밋 1에 포함한다.

## 2. 현재 구조 확인 (지시서 5절)

- `__root.tsx` `head()` — `meta` 10개(마지막이 `twitter:site` = `@Lovable`),
  `links` 2개(스타일시트, `favicon.ico`). `viewport` 에 `viewport-fit=cover`
  이미 있음. `apple-mobile-web-app-capable` 도 이미 있음.
- `Screen.tsx` — `Screen` / `Card` / `MascotSlot` 세 export. 톱니 버튼은
  `aria-label="설정"` 만 있고 `onClick` 이 없다. `Screen` 은 4개 라우트가 쓴다.
- `ListSheet.tsx` — 바텀시트 기준. `fixed inset-0 z-50 flex items-end
  justify-center` + 배경 `absolute inset-0 bg-foreground/30` 버튼 +
  본문 `relative w-full max-w-[420px] rounded-t-[20px] bg-background px-5 pt-5
  shadow-soft` + `paddingBottom: calc(env(safe-area-inset-bottom) + 1.5rem)`.
  제목은 `text-xl font-extrabold`, 닫기는 `X` 아이콘.
- `storage.ts` — `STORAGE_KEY = "studymate.state.v2"`. `loadState` / `saveState`
  모두 `typeof window === "undefined"` 가드 + `try/catch`.
- `StudyStore.tsx` — `useReducer(reducer, undefined, createInitialState)` 후
  마운트 시 `hydrate`, 상태 변경마다 `saveState`. 리로드하면 `loadState` 가
  시드 상태를 새로 만든다.
- 색 토큰은 `styles.css` `@theme inline` 에 등록된 것만 있다
  (`lilac / sky / lemon / blush / purple / purple-soft / task-* / check / up`).

## 3. 계획

### 커밋 1 — 설정: PWA 매니페스트와 아이콘 연결

파일: `public/manifest.webmanifest`(신규), 아이콘 PNG 4개(신규 추적),
`src/routes/__root.tsx`, `docs/orders/2026-08-22-pwa-install.md`(신규)

- 매니페스트는 지시서 6절 JSON 그대로.
- `head().meta` 에 `theme-color` · `apple-mobile-web-app-title` ·
  `apple-mobile-web-app-status-bar-style` 추가, `twitter:site` 제거.
- `head().links` 에 `manifest` · `apple-touch-icon` 추가.
- `viewport` · `apple-mobile-web-app-capable` · `favicon.ico` 는 그대로.
- `git add` 는 경로 명시. `public/drawable*` 는 넣지 않는다.

### 커밋 2 — 기능: 설정 시트 신설과 데이터 초기화

파일: `src/lib/storage.ts`, `src/components/app/SettingsSheet.tsx`(신규),
`src/components/app/Screen.tsx`

- `storage.ts` 에 `clearState()` 추가. `window` 가드 + `try/catch` 로
  기존 함수들과 같은 방식. 현재 키만 지우고 `v1` 은 남긴다.
- `SettingsSheet.tsx` 는 `ListSheet` 의 오버레이·라운드·여백·닫기 구조를
  그대로 따른다. 항목은 데이터 초기화 하나뿐, 문구는 지시서 3절 고정 문자열.
- 2단계 확인은 `useState` 로 같은 자리에서 전환. `confirm()` 안 쓴다.
- `네, 지울게요` → `clearState()` → `window.location.reload()`.
- 파괴적 동작이라 주 색상(보라)을 쓰지 않고 기존 토큰 `blush`(연분홍) 계열로
  잡는다. 새 색 하드코딩 없음.
- `Screen.tsx` 는 `useState` 로 열림 상태를 갖고 톱니에 `onClick` 만 붙인다.
  버튼 위치·크기·`aria-label` 무변경. 전역 상태 안 만든다.

### 커밋 3 — 문서: PWA 절 추가 및 프리즈 절 중복 정리

파일: `docs/CoreRules.md`, `docs/sessions/2026-08-22.md`

- 7장 iOS 대응에 매니페스트·아이콘 두 항목 추가.
- 7장에 설정 시트 한 줄 추가.
- 8장 프리즈 절의 중복된 "미구현" 안내 두 곳을 지시서가 준 한 줄로 합친다.
  규칙 목록 본문은 그대로.
- 12장 실행 명령을 `npm run dev` 기준으로 통일하고, `bun.lock` 이 있지만
  로컬에 bun 이 없어 npm 을 쓴다는 단서를 단다. `CLAUDE.md` 는 고치지 않는다.
- **(지시서 밖 · 사용자 지시)** 7장의 "마스코트와 배지 이미지는 아직 없다.
  원형 placeholder 로 자리만 잡는다" 를 아래 취지로 고친다.
  → 앱 아이콘은 실제 이미지가 들어왔고(7장 iOS 대응 참고), 마스코트·배지·
  트로피는 원본이 확보됐으나 아직 화면에 붙이지 않아 현재는 원형 placeholder
  로 자리만 잡으며, 반영은 별도 사이클에서 한다. 파일 목록과 슬롯 매핑은
  `CrossTrack_Assets.md` 참고.

### 지키는 것

- 라이브러리 추가 금지, 서비스워커 없음. 스택 버전 변경 없음.
- 6절 허용 파일 밖 무수정 — `index.tsx` / `calendar.tsx` / `stats.tsx` /
  `rewards.tsx` / `StudyStore.tsx` / `TabBar.tsx` / `ListSheet.tsx` 안 건드림.
- `public/drawable*` 커밋·삭제 안 함. `src/routeTree.gen.ts` 스테이징 안 함.
- 저장소 전체 `prettier --write` 안 돌림 — 새로 만들거나 고친 파일에만 적용해
  린트(Prettier 규칙)를 통과시킨다.
- 빌드·dev 서버 실행·push 안 함. 검증은 지시서 7절 8개만.

## 4. 지시서에 없는 것 — 보고만 하고 고치지 않음

| # | 위치 | 내용 |
|---|---|---|
| 1 | `__root.tsx` `RootShell` | `<html lang="en">` 인데 앱은 한국어이고 매니페스트는 `"lang": "ko"` 다. 홈 화면 추가·스크린리더에 영향이 있지만 6절 수정 범위 밖이라 그대로 둔다 |
| 2 | `CLAUDE.md` | 지시서 6절이 `CoreRules` 12장을 `npm run dev` 로 통일하라 하므로, `CLAUDE.md` 의 `bun run dev` 와 어긋난 채 남는다 (지시서 9절이 보고하라고 명시한 항목) |
| 3 | `CoreRules` 0장 낡은 내용 목록 | `ScreenDesign` 행에 "곰 마스코트 → **햄스터**" 가 있는데, 에셋이 실제로 들어온 지금은 낡은 목록이 아니라 확정 사실에 가깝다. 이번 범위 밖 |

## 5. 사용자 확인 결과 — 계획 승인 + 지시 1건 추가

1. **4절 1번(`<html lang="en">`)을 커밋 1에 포함해 `"ko"` 로 고친다.**
   매니페스트에 `"lang": "ko"` 를 넣으면서 HTML 을 `en` 으로 두면 같은 커밋
   안에서 모순이고, 스크린리더가 한국어 본문을 영어로 읽는 실제 문제가 있다.
   **`lang` 속성 한 곳만** 고치고 `RootShell` 의 나머지는 건드리지 않는다.
2. 4절 2번(`CLAUDE.md`)은 보고만 하고 그대로 둔다.
3. 4절 3번(0장 곰 마스코트 행)도 그대로 둔다. 그 표는 "다른 문서가 틀렸다"는
   표이고, `ScreenDesign.md` 가 곰이라고 적은 것은 **여전히 틀린 사실**이다.
   에셋이 들어왔다고 해서 그 행이 무효가 되는 게 아니다. (내 판단이 틀렸다.)

## 6. 실행 기록 — 두 번째 사이클

### 커밋 1 — `68ac5bc` 설정: PWA 매니페스트와 아이콘 연결

파일: `public/manifest.webmanifest`(신규), 아이콘 PNG 4개(신규 추적),
`src/routes/__root.tsx`, `docs/orders/2026-08-22-pwa-install.md`(신규)

- 매니페스트는 지시서 6절 JSON 그대로. `lang: "ko"`, 색은 `#F4F1FA`.
- `head().meta` — `theme-color` · `apple-mobile-web-app-title` ·
  `apple-mobile-web-app-status-bar-style` 추가, `twitter:site` 제거.
  `viewport` · `apple-mobile-web-app-capable` 은 이미 있어 그대로 뒀다.
- `head().links` — `manifest` · `apple-touch-icon` 추가. `favicon.ico` 유지.
- `RootShell` 의 `<html lang="en">` → `"ko"` (승인 1번). `lang` 속성 한 곳만
  고쳤고 나머지는 건드리지 않았다.
- `public/drawable*` 는 넣지 않았다.

> **prettier 처리** — `__root.tsx` 는 **수정 전부터** `prettier --check` 를
> 통과하지 못했다. 확인해 보니 파일이 CRLF 라 전 줄이 걸리는 것이고,
> 줄바꿈을 빼고 비교하면 내용 차이는 0이다. `--write` 를 돌리면 전 줄이
> diff 에 잡혀 무관한 변경이 되므로 **돌리지 않았다.** 지시서 7절 #8 이
> 예고한 "기존 CRLF 경고"에 해당한다.

### 커밋 2 — `39482e8` 기능: 설정 시트 신설과 데이터 초기화

파일: `src/lib/storage.ts`, `src/components/app/SettingsSheet.tsx`(신규),
`src/components/app/Screen.tsx`

- `clearState()` 추가. `window` 가드 + `try/catch` 로 기존 두 함수와 같은 방식.
  `removeItem(STORAGE_KEY)` 로 현재 키만 지우고 옛 `v1` 은 남긴다.
- `SettingsSheet.tsx` 는 `ListSheet` 의 오버레이(`bg-foreground/30`) ·
  `rounded-t-[20px]` · `px-5 pt-5` · `env(safe-area-inset-bottom)` 패딩 ·
  `X` 닫기 버튼 구조를 그대로 따랐다. 새 패턴 없음.
- 2단계 확인은 `useState` 로 같은 자리에서 전환. `confirm()` 안 씀.
- 초기화 버튼은 파괴적 동작이라 주 색상(보라)을 피하고 기존 토큰 `blush`,
  취소는 `muted` 로 잡았다. 새 색 하드코딩 없음.
- `Screen.tsx` 는 로컬 `useState` 와 톱니 `onClick` 만 추가. 버튼 위치·크기·
  `aria-label` 무변경. 전역 상태 안 만들었다.

### 커밋 3 — 문서: PWA 절 추가 및 프리즈 절 중복 정리

파일: `docs/CoreRules.md`, `docs/sessions/2026-08-22.md`

- 7장 iOS 대응에 매니페스트·아이콘 2항목 추가.
- 7장 탭 절에 설정 시트 한 줄 추가.
- 8장 프리즈 절의 겹치던 "미구현" 안내 두 곳을 한 줄로 합쳤다. 규칙 목록 무변경.
- 12장에 bun 미설치로 `npm run dev` 를 쓴다는 단서 추가.
- **(승인 지시)** 7장 톤 절의 "마스코트와 배지 이미지는 아직 없다" 를
  아이콘은 반영됨 / 마스코트·배지·트로피는 원본 확보, 화면 반영은 별도
  사이클 / 매핑은 `CrossTrack_Assets.md` 참고로 고쳤다.

> 커밋 3의 해시는 이 파일이 그 커밋에 포함되므로 여기에 적을 수 없다.

## 7. 검증 결과 (지시서 7절)

| # | 확인 | 결과 |
|---|------|------|
| 1 | 매니페스트 유효 JSON | **통과.** `node` 파싱 OK — name=공부체크 StudyMate, lang=ko, icons=3 |
| 2 | 매니페스트 아이콘 3개 실재 | **통과.** `icon-192.png` · `icon-512.png` · `icon-maskable-512.png` |
| 3 | `apple-touch-icon.png` 실재 | **통과** |
| 4 | `head()` 에 manifest·apple-touch-icon·theme-color | **통과.** 87행 `theme-color`, 102행 `rel: "manifest"`, 103행 `rel: "apple-touch-icon"` |
| 5 | `twitter:site` 사라짐 | **통과.** `grep -rn "twitter:site" src/` 결과 없음 |
| 6 | `localStorage` 가 `storage.ts` 밖에 없음 | **통과.** 54·71·84행 세 곳 모두 `storage.ts` |
| 7 | `npx tsc --noEmit` | **통과.** 오류 없음 |
| 8 | `npm run lint` | **기존 CRLF 외 새 오류 없음.** 아래 설명 |

### 8번 상세

저장소 전체 린트는 6134개(오류 6127)가 나오는데 **6127개 전부가
`Delete ␍`(CRLF)** 다. 이번에 손댄 4개 파일만 린트하면 CRLF 를 뺀 결과는
`Screen.tsx 61:2 Insert ⏎` 하나뿐이고, 이것은 **파일 끝 개행이 없다**는
뜻으로 커밋 전 원본(`git show HEAD:src/components/app/Screen.tsx`)에도
그대로 있던 **기존 오류**다. 새 파일 `SettingsSheet.tsx` 는 오류 0이다.

## 8. 완료 조건 보고 (지시서 9절)

- `CLAUDE.md` 는 여전히 `bun run dev` 로 적혀 있고 `CoreRules` 12장은
  `npm run dev` 로 통일했다. **어긋난 채 남았다** — 지시서가 이번에 고치지
  말라고 했으므로 손대지 않았다.
- `public/drawable/`·`public/drawable-nodpi/` 는 미추적 그대로, 세 커밋 어디에도
  들어가지 않았다. 삭제도 하지 않았다.
- `src/routeTree.gen.ts` 는 내용 차이 0인 채 미스테이징.

### 이번 사이클에서 새로 보고할 것

| # | 위치 | 내용 |
|---|---|---|
| 1 | `src/components/app/Screen.tsx` 끝 | 파일 끝 개행이 없어 린트 오류 1개가 남아 있다(기존). 허용 파일이라 고칠 수는 있었지만 이번 지시 범위 밖이라 그대로 뒀다 |
| 2 | 저장소 전체 | 소스가 CRLF 라 `npm run lint` 가 6127개 오류를 낸다. `.gitattributes` 나 `endOfLine: "auto"` 로 한 번에 정리할 수 있지만 이번 범위 밖 |
| 3 | `src/routes/index.tsx:12` | 4개 라우트가 각자 `head()` 로 `description`·`og:*` 를 덮어쓴다(라우트별 override 자체는 정상). 다만 문구가 루트와 갈린다 — 루트는 "공부 습관 체크 **웹앱**", index 는 "공부 습관 **체크앱**". 매니페스트 `description` 은 루트 쪽을 따랐다. 카피 통일이 필요하지만 이번 범위 밖 |

